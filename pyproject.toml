[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="redirectaudit"
version="0.1.0"
requires-python=">=3.10"
description="Small HTTP redirect-chain auditor"
[project.scripts]
redirectaudit="redirectaudit.cli:main"
